package dreamwisp.audio {	import flash.media.SoundChannel;		/**	 * ...	 * @author Brandon	 */		//TODO: For handling events of any types, such as sound, GameScreens	//		get a series of 'component' signals (like Entitys currently have)	//		and the main Game or ScreenManager would handle listeners for each	//		of these. So when a Screen hits a button it also dispatches madeSound	//		and then it goes back to the BaseGame where both are handled - one changes	//		the screen to the new destination and the other plays a sound in the 	//		SoundSystem.	public class SoundSystem 	{		private const soundChannel:SoundChannel = new SoundChannel();				public function SoundSystem() 		{					}			}}